# ================================
# 闲鱼卖家 —— 打包构建依赖
# ================================
# 仅在把项目编译成独立二进制（exe / 单文件发布）时需要。
# 正常部署（Docker、源码运行）不要装这些：它们会拉起 C 编译工具链，
# 在 NAS、软路由等低配设备上足以把 CPU 和内存吃满。
#
# 用法：
#   pip install -r requirements.txt        # 运行服务
#   pip install -r requirements-build.txt  # 额外，只有要打包时才装

nuitka>=2.7
ordered-set>=4.1.0
zstandard>=0.22.0
