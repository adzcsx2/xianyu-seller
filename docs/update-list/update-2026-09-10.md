# 更新详情 - 2026-09-10

## 执行批次 - 19:10

**触发方式**：技能调用；工作区 diff 审计、最近提交补充分析和文档基线生成
**来源范围**：当前工作区文档 diff；`d122dfa`、`ed62d61`、`c18d5b6`、`88a1ead` 相关源码和测试；当前 `.ai/index/backend-apis.json`
**关联提交**：`d122dfa`、`ed62d61`、`c18d5b6`、`88a1ead`
**受影响文档**：根 README、docs 索引、项目概览、架构、界面、接口、依赖、部署、迁移、接口目录、CHANGELOG、更新详情、API 索引和元数据

## 审计结论

- 初始审计时工作区无 diff；文档生成期间出现的四处 AI 相关源码/测试改动随后由并发流程提交为 `c18d5b6` 和 `88a1ead`，已按真实提交归档。
- 项目属于 Python/FastAPI + React/Vite 通用项目；后端入口是 `Start.py` / `app/reply_server.py`，前端入口是 `frontend/App.tsx`。
- `d122dfa` 的用户可见安全变化是：模型地址增加协议/URL/网络地址校验并禁止重定向；系统模型列表收紧为管理员；普通 AI 设置响应不再回传 API Key 原文。
- `ed62d61` 的当前架构基础是持久化功能开关、依赖收敛、环境变量 AI 配置覆盖和前后端开关 registry。
- API 索引脚本重建后得到 255 条源码路由；新增 `/ai-models`，并刷新 `app/reply_server.py` 的来源行号。

## 变更证据矩阵

| 源文件 | 变更符号/入口 | 实际变化 | 用户可见影响 | 目标文档 |
| --- | --- | --- | --- | --- |
| `app/ai_models.py` | `validate_model_service_url()`、`fetch_available_models()` | 校验 HTTP(S)、禁止凭据/query/fragment；解析并拒绝本机/内网等地址；模型请求不跟随重定向。 | 无效模型地址在发起请求前失败，降低服务端请求伪造和凭据外泄风险。 | `docs/modules/ARCHITECTURE.md`、`docs/references/API.md`、`docs/deployment.md` |
| `app/ai_reply_engine.py` | `AIReplyEngine` 客户端创建 | 创建 OpenAI 兼容客户端前复用模型服务地址校验。 | AI 回复不会直接使用未通过网络地址校验的服务地址。 | `docs/modules/ARCHITECTURE.md`、`docs/references/API.md` |
| `app/reply_server.py` | `get_ai_models()` | 无 `cookie_id` 的系统模型查询要求管理员；账号查询校验账号归属；上游失败统一为不泄露细节的 502。 | 普通用户不能探测系统模型服务，账号模型列表保持归属隔离。 | `docs/references/API.md`、`docs/modules/INTERFACES.md` |
| `app/reply_server.py` | `_public_ai_reply_settings()`、AI 设置 GET 路由 | API Key 默认只返回已配置状态；管理员受保护响应才可查看部署级密钥。 | 普通用户页面不再获得密钥原文。 | `README.md`、`docs/modules/ARCHITECTURE.md`、`docs/deployment.md`、`docs/MIGRATION.md` |
| `app/feature_flags.py` | `FEATURE_FLAG_REGISTRY` | 定义开关、依赖、UI 目标和风险等级；当前源码移除的仅是空白格式。 | 支撑页面隐藏、后台任务收敛和服务端动作门禁的统一合同。 | `docs/modules/ARCHITECTURE.md`、`docs/modules/INTERFACES.md` |
| `app/ai_config.py` | `resolve_ai_config()`、`apply_env_ai_settings()` | 账号/系统设置优先，空字段使用 `API_KEY`、`MODEL_BASE_URL`、`MODEL_NAME` 环境变量；响应只标记覆盖来源。 | 部署者可提供默认 AI 配置，且页面不需暴露环境变量值。 | `docs/references/DEPENDENCIES.md`、`docs/MIGRATION.md` |
| `frontend/contexts/FeatureFlagsContext.tsx`、`frontend/lib/featureRegistry.ts` | 前端开关快照与 UI registry | 前端仅镜像页面/控件归属，页面根据 `effective` 状态显示或回退。 | 功能开关关闭后页面与控件一致隐藏。 | `docs/modules/INTERFACES.md` |
| `tests/test_ai_models.py`、`tests/test_ai_reply.py` | 安全与权限测试 | 覆盖内网 URL、非 HTTP URL、重定向、系统列表权限和 API Key 展示合同。 | 文档可将上述行为作为已有测试保护的合同。 | `docs/references/API.md`、`docs/guide/自动化测试指南.md` |
| `app/reply_server.py` | `test_ai_reply()` | `88a1ead` 将 AI 测试无有效回复从 400 改为 502，并使用稳定错误文案。 | 上游服务失败被正确表达为网关错误，且不泄露供应商细节。 | `docs/references/API.md` |
| `frontend/lib/request.ts` | Axios 响应拦截器 | `88a1ead` 优先读取响应 `detail` 或 `detail.message` 覆盖错误消息。 | 后端结构化错误能在页面显示为可读提示。 | `docs/references/API.md` |
| `tests/test_ai_env_priority.py` | AI 环境变量优先级测试 | `c18d5b6` 增加前端请求错误详情与消息归一化断言。 | AI/功能开关错误能以用户可读文本呈现。 | `docs/references/API.md` |
| `tests/test_product_knowledge.py` | `AIReplyTestRouteTests` | `c18d5b6` 增加 AI 测试上游无回复时的 502 和稳定错误文案断言。 | AI 测试失败不会伪装成成功，也不向前端泄露上游细节。 | `docs/references/API.md` |
| `app/brand_migration.py` | 旧默认服务值常量 | 将兼容匹配字符串拆分，运行时仍只清理冻结的旧默认值。 | 没有新的用户行为变化；保留迁移说明中的安全边界。 | `docs/MIGRATION.md` |

## 实际文档变更详情

### 根 README 与 docs 索引

- 新增文档导航和最近更新表，链接到统一索引与变更日志。
- 新增 `docs/README.md`，覆盖实际存在的 `guide`、`modules`、`references`、`reports`、`plan`、`testing` 等分类。

### 项目、架构与界面文档

- 新增 `PROJECT_OVERVIEW.md`，记录运行入口、主要目录、核心边界和典型链路。
- 新增 `ARCHITECTURE.md`，记录 FastAPI/React/SQLite/后台任务关系、功能开关依赖模型和 AI 安全流。
- 新增 `INTERFACES.md`，记录页面 ID、开关归属、关闭语义、高风险交互和前端兼容约束。

### API、依赖与运维文档

- 新增 `API.md`，记录 `/health`、登录、`/feature-flags`、AI 设置和 `/ai-models` 的认证、错误与副作用边界。
- 新增 `DEPENDENCIES.md`，记录运行时依赖、版本约束、配置优先级和 Docker 变体。
- 更新部署与迁移说明，补充模型 URL 校验、管理员模型列表权限和 API Key 脱敏规则。
- API 摘要同步记录当前工作区测试合同：AI 测试上游无有效回复时返回稳定 502，前端请求层展示服务端 detail。
- 重建 `.ai/index/backend-apis.json`；本次生成结果为 255 条源码路由，并包含 `/ai-models`。

## Git 提交详细分析

### c18d5b6 - AI 回复失败回归测试

**变动文件**：

- `tests/test_ai_env_priority.py`
  - 增加前端请求层读取 `detail` 和覆盖错误消息的合同断言。
- `tests/test_product_knowledge.py`
  - 增加 AI 测试无有效回复时返回 502 和稳定错误文案的回归测试。

### 88a1ead - 改善 AI 回复失败提示

**变动文件**：

- `app/reply_server.py`
  - 将 AI 测试无回复的错误从 400 调整为 502，并更新为不泄露上游细节的提示。
- `frontend/lib/request.ts`
  - 在 Axios 响应拦截器中优先映射字符串或对象形式的 `detail`。

### d122dfa - 当前安全修复提交

**变动文件**：

- `app/ai_models.py`
  - 新增模型服务 URL 解析、主机地址解析和私有地址拒绝。
  - 模型列表请求关闭重定向，并将重定向作为业务错误处理。
- `app/ai_reply_engine.py`
  - 创建 AI 客户端前调用统一 URL 校验。
- `app/brand_migration.py`
  - 拆分旧品牌服务默认值字符串，迁移匹配语义保持不变。
- `app/feature_flags.py`
  - 删除文件末尾空白行，无业务行为变化。
- `app/reply_server.py`
  - 增加系统模型列表管理员权限和账号归属校验。
  - 增加管理员限定的 API Key 展示参数，普通响应继续脱敏。
- `tests/test_ai_models.py`
  - 增加 URL、重定向、系统模型权限和 API Key 脱敏测试。
- `tests/test_ai_reply.py`
  - 将 API Key 展示合同改为显式管理员场景测试。
- `tests/test_brand_identity.py`、`tests/test_brand_service_migration.py`
  - 调整旧品牌字符串的测试匹配方式，无用户行为变化。

### ed62d61 - 功能开关与 AI 配置迁移基础

本次文档按当前源码记录该提交形成的架构合同：`app/feature_flags.py`、`app/ai_config.py`、`app/ai_models.py`、`app/reply_server.py`、`XianyuAutoAsync.py`、前端 Feature Flags context/registry、系统设置页面、Compose/.env 配置和对应测试共同形成独立功能开关与 AI 环境覆盖链路。详细文件级变更以该提交 diff、当前源码和测试合同为事实源。

## 未覆盖风险

- 当前仓库没有独立 OpenAPI JSON；全量接口目录中的部分 schema、handler auth/owner 和副作用仍需要以运行时 `/openapi.json` 与源码联合核验。
- 已运行并通过 `test_ai_models.py`、`test_ai_env_priority.py`、`test_product_knowledge.py` 三个聚焦测试文件；本次未运行完整测试套件或启动真实外部平台服务，文档不等于真实外部平台验证通过。
- 现有历史计划、TDD 文档和接口目录内容仍可能包含提交时的行号或旧计数；后续接口变更应重新运行 API 索引脚本并同步摘要。

## 执行批次 - 20:37

**触发方式**：技能调用；元数据之后的已提交源码改动审计
**来源范围**：`994e552^..994e552` 的 `app/ai_models.py`、`tests/test_ai_models.py`
**关联提交**：`994e552`
**受影响文档**：`docs/modules/ARCHITECTURE.md`、`docs/references/API.md`、`docs/deployment.md`、`docs/MIGRATION.md`、`README.md`、`docs/reports/CHANGELOG.md`

## 变更证据矩阵

| 源文件 | 变更符号/入口 | 实际变化 | 用户可见影响 | 目标文档 |
| --- | --- | --- | --- | --- |
| `app/ai_models.py` | `_PUBLIC_DNS_MAPPING_NETWORK`、`validate_model_service_url()` | 对 DNS 主机名解析到 `198.18.0.0/15` 的托管运行时映射不再按私有地址拒绝；字面量 IP 不享受该例外。 | 公网模型服务在托管运行时可继续通过地址校验，同时保留对直接内网/本机 IP 的 SSRF 防护。 | `docs/modules/ARCHITECTURE.md`、`docs/references/API.md`、`docs/deployment.md`、`docs/MIGRATION.md` |
| `tests/test_ai_models.py` | `test_public_dns_mapping_range_is_allowed_for_external_model_service()` | 新增公网 DNS 映射地址放行测试，并验证仍向原始域名 `/models` 发起请求且不跟随重定向。 | 为托管运行时兼容行为和请求参数提供回归保护。 | `docs/references/API.md` |

## 实际文档变更详情

### AI 模型服务地址校验

- 架构、接口、部署和迁移文档均补充：托管运行时将公网 DNS 结果映射到 `198.18.0.0/15` 时，仅 DNS 主机名可使用该限定例外。
- 明确直接填写字面量 IP 仍按本机/内网地址规则拒绝，未放宽既有 SSRF 防护。
- README 最近更新、CHANGELOG 当日摘要同步反映该兼容规则。

## Git 提交详细分析

### 994e552 - 兼容公网模型服务的运行时 DNS 映射

**变动文件**：

- `app/ai_models.py`
  - 新增 `198.18.0.0/15` 运行时 DNS 映射网络常量。
  - 区分字面量 IP 与 DNS 主机名；仅 DNS 主机名命中该映射网段时跳过私有地址拒绝。
- `tests/test_ai_models.py`
  - 新增 `api.deepseek.com` 解析到 `198.18.39.6` 时成功获取模型列表的回归测试。

## 未覆盖风险

- 本批次未启动真实公网模型服务；文档仅记录源码和回归测试已覆盖的地址校验行为。
- `198.18.0.0/15` 的放行依赖托管运行时的 DNS 映射约定；其他部署环境仍应按实际 DNS 和网络拓扑验证。

## 执行批次 - 21:10

**触发方式**：用户请求；重写产品 README、补充前端截图、升级软件版本并调整前端主题
**来源范围**：`README.md`、`frontend/`、`app/announcement.py`、`frontend/package.json`、构建后的 `static/`
**关联版本**：`1.1.0`
**受影响文档**：根 README、项目概览、文档更新日志、本文更新详情和前端总览截图

## 本批次变更详情

- 根 README 改为以前端功能为主，重点介绍自动 AI 聊天、AI 回复、知识库、消息中心、账号、商品、订单、卡密、通知和系统设置。
- README 明确说明：阿奇索可继续负责自动发货，本项目用于补充其未覆盖的自动聊天能力；问题反馈统一提交 GitHub Issue。
- 新增 `docs/screenshots/dashboard.png`，截图使用示例数据并脱敏，不包含真实商品标题、订单号或账号标识。
- 软件版本更新为 `1.1.0`，同步 `app/announcement.py`、前端 `package.json/package-lock.json` 和页面标题。
- 前端品牌主色由原主题色调整为淡蓝色，覆盖主题变量、主要操作按钮、侧边栏、图表、favicon 与部署构建产物；错误、危险和警告状态色仍保持语义色。

## 验证证据

- 通过 Vite 生产构建：2371 个模块转换成功，生成 `static/` 构建产物。
- 使用本地临时服务打开总览页面，确认页面标题为“闲鱼卖家 - 自动 AI 聊天工作台”、主界面正常渲染且浏览器控制台错误数为 0。
- 截图生成前对页面中的长数字、日期、商品标题和金额进行了示例化处理。

## 未覆盖风险

- `npm run build` 在当前 Windows shell 中因 `node` 未进入 npm 子进程 PATH 失败；使用已安装的 Node 可执行文件直接运行同一清理脚本和 Vite 构建已成功。
- 本次未验证真实闲鱼账号、阿奇索接口或外部 AI 服务；README 中的搭配说明是使用边界说明，不代表项目已实现两者的 API 集成。
