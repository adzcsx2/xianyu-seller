# Passistant 已发布商品配置与接口规则补全执行文档

[返回 README](./README.md) · [下一篇：架构设计](./01-架构设计.md) · [开发规范](./02-开发规范.md) · [修复路线图](./03-修复路线图.md)

本文件是唯一进度事实源。后续模型必须从这里恢复，不得只凭聊天摘要或猜测运行状态。

## 进度指针

<!-- progress-pointer:start -->
```yaml
current_phase: 4
current_phase_status: complete
last_updated: 2026-09-06T15:58:00.000Z
last_actor: main-agent
last_commit: 6dce5a6
next_action: 任务完成
blockers: []
parallelizable_groups: null
```
<!-- progress-pointer:end -->

## 恢复协议

1. 先读本文件进度指针、最新执行日志和当前阶段的全部任务。
2. 使用 `/ecc:execute-doc` 执行；阶段内和阶段间均串行，`parallelizable_groups` 始终为 `null`。
3. 每个 `P<N>.1` 只允许读取与记录；除了创建临时 API 登录会话，不得修改业务状态。
4. 从第一个未勾选任务开始。每完成一项，勾选任务、更新进度指针，并在执行日志顶部追加不含秘密的证据。
5. 每个写入前都先 GET/查询并判断幂等状态；不能证明安全时停止，不得“先写再看”。
6. 遇到阻断，将 `current_phase_status` 设为 `blocked`，在 `blockers` 写明可操作原因并停止。
7. 只有当前阶段所有任务及门禁通过后才能进入下一阶段。不得因一次 2xx 就宣布阶段成功。
8. 每阶段按 `/ecc:execute-doc` 的审计与本地提交规则执行；本计划不授权 push。

## 执行边界

- 目标仓库：`D:\github\xianyu-super-butler`。
- Passistant 仓库：`E:\exilecore2\Passistant`，仅用于读取产品事实和通过 `Manage-ActivationCodes.ps1` 查询/核验既有批次。
- 所有 D 项目业务状态只通过 `http://localhost:8080` 的已验证项目 API 修改。禁止浏览器、Playwright、桌面自动化、开发者工具注入和直接数据库写入。
- 唯一商品 ID 是 `1081710901648`。不得调用任何发布能力、不得创建或复制第二件商品，也不得为“补数据”调用 `POST /items` 伪造未同步商品。
- `POST /items` 是本地记录接口，不是闲鱼发布接口。项目当前没有外部商品创建/发布 API，这是能力缺失而不是漏扫。
- 卡密批次 `XYB-PASSISTANT-30D-20260906-1081710901648` 已经生成。严禁再次运行 `Generate`，包括 preview。
- 管理员用户名为 `admin`。密码只能从安全执行上下文读取；不得写入 Markdown、源码、命令输出或执行日志。若后续会话拿不到用户提供的密码，立即 blocked，不得猜测或复用浏览器 `localStorage`。
- DeepSeek `API_KEY`、`MODEL_NAME`、`MODEL_BASE_URL` 从 D 仓库根 `.env` 读取，仅在执行进程内传给账号设置 API。不得输出、复制或修改这些值。
- token、Cookie 和完整激活码只在当前进程内使用，不进入文档或测试输出。
- 产品固定为 30 天 ¥9.9，只支持国际服和台服，不支持国服；每单发送 1 个唯一激活码。
- 图片仅作历史证据。不得上传、编辑或通过 API 再发布图片。
- 本计划不授权 Git push、PR、tag、第三方部署、凭据修改或 Passistant 正式发版。

## 需求追踪

| ID | 最新用户事实/请求 | 实现任务 | 验收证据 | 当前状态 |
|---|---|---|---|---|
| REQ-01 | 只卖已发布商品 `1081710901648`，不能有第二件 | P1.1～P1.3、P4.4 | 账号/商品唯一性回读；TC-ITEM-01～06、TC-E2E-01 | PASS |
| REQ-02 | 经 API 同步并确认该商品的账号归属 | P1.1～P1.3 | `GET /cookies/details`、同步结果和 `GET /items`；TC-AUTH-01～03、TC-ITEM-01～04 | PASS |
| REQ-03 | 100 个 30 天月卡已生成，后续核验并导入卡密库存 | P2.1～P2.2 | QueryBatch/ListCodes/ValidateExport、TXT/CSV/card 集合摘要；TC-CODE-01～07、TC-CARD-01～04 | PASS |
| REQ-04 | 唯一商品自动发货，每单 1 码 | P2.3 | delivery config 回读 `configured=true`、`complete=true`；TC-DELIVERY-01～04 | PASS |
| REQ-05 | DeepSeek Key/模型/地址已在根 `.env` 配置，后续直接使用并按商品问题回复 | P3.1～P3.3 | 环境键存在性、账号设置回读、五类隔离测试；TC-AI-01～10 | PASS |
| REQ-06 | ¥9.9；支持国际服/台服，不支持国服 | P1.3、P3.2～P3.3 | 商品事实和提示词断言；TC-ITEM-05～06、TC-AI-03/07 | PASS |
| REQ-07 | 全流程使用项目 API，不操作浏览器或电脑 | 全阶段 | 调用清单与代码变更审计；TC-E2E-01～05 | PASS |
| REQ-08 | 汇总全部接口到单个 AI 规则，并解释接口“缺失”原因 | P4.1～P4.4 | 最终 OpenAPI/索引/MD 集合一致；TC-API-01～08 | PASS |
| REQ-09 | 保留商品管理前端全功能审计结论，明确面板不能发布商品 | P4.1、P4.3 | `05-全量接口目录.md` 和 API-first 规则说明；TC-UI-01～05 | PASS |

最终阶段必须把状态更新为 `PASS`、`BLOCKED`、`not verified` 或带证据的部分完成状态，不能笼统写“完成”。

## 子代理路由

技术栈：Python 3.11+/FastAPI/SQLite + React 19/TypeScript/Vite + PowerShell 7。后续若 `/ecc:execute-doc` 按阶段分派同模型代理，仍必须逐阶段串行。

| 阶段/工作 | 首选角色 | 当前解析 | Parallel Group | 备注 |
|---|---|---|---|---|
| Phase 1：API 登录、同步、唯一归属 | API 执行/审计 | `main-agent` 回退 | none | 共享 token、cookie_id 和商品状态 |
| Phase 2：卡密核验、卡库存、发货绑定 | 运维/API 执行 | `main-agent` 回退 | none | 明文库存与生产批次不可并行处理 |
| Phase 3：DeepSeek 账号配置与测试 | AI 配置/API 审计 | `main-agent` 回退 | none | 依赖唯一 cookie_id 和根 `.env` |
| Phase 4：索引、全接口规则、总验收 | Python/文档审查 | `main-agent` 回退 | none | 依赖最终源码和业务回读 |

不允许任何并行组；所有步骤共享生产账号或生成文件，必须保持 `parallelizable_groups: null`。

## Phase 1 — API 登录、同步并唯一绑定既有商品

依赖：无  
阶段验收：通过 API 确认一个属于当前用户的 `cookie_id` 与商品 `1081710901648` 唯一匹配；未创建任何新商品或本地假记录。

- [x] **P1.1 只读登录并记录运行基线**
  - 先读：`AGENT.md`、`CLAUDE.md`、`docs/references/ai-rules/03-后端接口优先.md`；`app/reply_server.py` 中 `login`、`verify`、`get_cookie_details`、`get_all_items`、`get_cards` 和 `get_all_ai_reply_settings`。
  - 操作：用用户名 `admin` 和安全上下文密码调用 `POST /login`，token 仅保存在当前进程内；随后调用 `GET /verify`、`GET /cookies/details`、`GET /items`、`GET /cards`、`GET /ai-reply-settings`。不做业务写入。
  - 记录：只写 HTTP 状态、用户 ID、账号 ID/启用状态摘要、目标商品出现次数、卡组名称/ID/条数摘要、AI `api_key_configured` 状态。不得记录密码、token、Cookie、Key 或完整卡密。
  - 绿色：登录成功、`verify.authenticated=true`、管理员身份符合预期，所有读取端点可用。
  - 阻断：安全上下文没有密码、登录失败、返回账号归属不清或任何读取端点返回无法解释的 5xx。
  - 完成：执行日志包含可复核的非敏感基线，且业务数据未变化。

- [x] **P1.2 同步目标账号并定位商品**
  - 先读：P1.1 账号列表；`get_all_items_from_account` 的 owner 校验和返回合同。
  - 操作：若 `GET /items` 已把目标商品唯一映射到一个 owned/enabled `cookie_id`，不调用同步；否则对候选 owned/enabled 账号逐一串行调用 `POST /items/get-all-from-account`，每次后都 `GET /items`。
  - 约束：只同步已有闲鱼商品；不得调用 `POST /items`，不得填写虚构 title/price/image 来制造目标记录。
  - 绿色：目标 ID 恰好出现一次，并且所属账号存在、属于当前用户且启用。
  - 阻断：所有同步结束后仍找不到目标商品；目标出现在多个账号；多个记录无法判定唯一；同步遇到 Cookie 空、风控、502 或账号失效。
  - 完成：进度指针和日志记录唯一 `cookie_id` 与同步结果摘要。

- [x] **P1.3 核验唯一商品合同**
  - 先读：`GET /items` 返回的目标记录；前端 `ItemList` 的展示字段和 item source 语义。
  - 操作：只读核对 item ID、title、price、source、状态、图片 URL 摘要和所属账号。不得编辑、擦亮、删除、上传图片或发布。
  - 绿色：ID 为 `1081710901648`；价格为 `9.9` 或可明确解析为 ¥9.9；没有第二个由本计划产生的 Passistant 商品。
  - 说明：若标题/描述未完整包含“国际服/台服、国服不支持”，记录为 AI 知识约束，不使用缺失的外部编辑 API 修改闲鱼商品。
  - 阻断：价格不是 ¥9.9、目标已下架/不可售、商品归属不一致或出现疑似重复 Passistant 商品，需要用户手动确认。
  - 完成：唯一商品事实写入执行日志，REQ-01/02/06 更新证据。

## Phase 2 — 核验既有批次、导入卡密并绑定自动发货

依赖：Phase 1  
阶段验收：已生成批次、TXT、CSV 和 D card 精确对应 100 个 30 天码；唯一商品启用一个普通单规格、每单 1 码的完整发货配置。

- [x] **P2.1 只读核验已生成的生产批次与导出**
  - 先读：`E:\exilecore2\Passistant\.agents\skills\manage-activation-codes\SKILL.md`、`references/database-contract.md` 和脚本参数；再次确认批次号完整为 `XYB-PASSISTANT-30D-20260906-1081710901648`。
  - 操作：只运行 `QueryBatch`、`ListCodes`、`ValidateExport`；安全解析同名 TXT/CSV，比较数量、唯一性、duration、batch、lowercase SHA-256 和五字符 suffix。不得运行任何 `Generate`、`ExportBatch` 或直接数据库查询。
  - 绿色：batch quantity/audit/code count 均为 100、`duration_days=30`、`channel=xianyu`、可售状态符合预期；TXT 100 个非空唯一值；CSV 100 行且集合与 TXT 完全一致；所有 hash/suffix 与数据库元数据一致。
  - 阻断：导出缺失、ValidateExport 失败、数量/集合/hash/suffix/批次/期限任一不一致。先停止后续 card/delivery 写入，运行精确 `DisableBatch` preview；只有得到明确授权后才可带 `-Execute` 禁用 remaining available inventory。绝不重新生成。
  - 完成：日志只记录批次号、数量、状态、文件名/长度、验证结果和摘要 hash，不记录完整码。

- [x] **P2.2 幂等复用或创建一个卡密库存**
  - 先读：P1.1 的 `GET /cards`、P2.1 验证结果、`create_card`/`get_card` handler 和 owner 过滤。
  - 操作：寻找名称/描述含完整批次号的 card，逐个 `GET /cards/{id}`。若恰有一个 `type=data`、enabled、`is_multi_spec=false` 且 100 个非空行与 TXT 集合完全相等，则复用；若不存在，调用一次 `POST /cards` 创建。建议名称 `Passistant 30天月卡-1081710901648`，description 写完整批次号，`data_content` 为验证后的 100 行。
  - 约束：比较在进程内完成，输出只显示数量/集合摘要；不得打印 request body。已有 card 若部分匹配、行数不同、重复或内容不同，立即 blocked，不更新、拼接或再建第二个 card。
  - 绿色：唯一 card 可回读，`type=data`、enabled、单规格，非空行恰 100 且集合相等。
  - 完成：日志记录 card ID、名称、计数和复用/创建结果。

- [x] **P2.3 保存并回读单规格每单一发货配置**
  - 先读：`GET /items/{cookie_id}/{item_id}/delivery-config` 当前结果；`ItemDeliveryConfigIn`、`ProductVariantBindingIn` 和 `save_item_delivery_config`。
  - 操作：若现有配置已精确满足目标则不 PUT；否则调用一次 `PUT /items/{cookie_id}/1081710901648/delivery-config`，保存 `enabled=true`、`is_multi_spec=false`，且 variants 恰有一项：`card_id=<P2.2>`、`delivery_count=1`、`enabled=true`、`binding_enabled=true`、`source=manual`。
  - 绿色：再次 GET 返回 `configured=true`、`complete=true`、`enabled=true`、`is_multi_spec=false`、`variant_count=1`、`configured_count=1`，且 card ID/发货数量正确。
  - 阻断：商品/card 404、owner 403、配置冲突 409、返回不完整或出现多个 variant。不得用通用规则掩盖专属配置失败。
  - 完成：日志记录配置摘要，REQ-03/04 更新证据。

## Phase 3 — 从根 .env 应用并验证 DeepSeek 商品回复

依赖：Phase 2  
阶段验收：唯一账号的 AI 设置使用根 `.env` 中的 Key/模型/地址，商品提示词完整，五类隔离测试通过。

- [x] **P3.1 只读核验 .env 和当前账号 AI 配置**
  - 先读：根 `.env` 中 `API_KEY`、`MODEL_NAME`、`MODEL_BASE_URL`、`AI_REPLY_ENABLED` 的存在性；`AIReplySettings`、`_public_ai_reply_settings`、`update_ai_reply_settings`、`ai_reply_test` 和 `_resolve_system_prompt`。
  - 操作：安全解析 `.env`，只记录各键“存在且非空/缺失”，不输出值；`GET /ai-reply-settings/{cookie_id}`，记录非敏感字段和 `api_key_configured`。不写入设置。
  - 已知：`API_KEY`、`MODEL_NAME`、`MODEL_BASE_URL` 当前存在且非空；`MODEL_NAME` 已核验为用户指定模型。静态源码未发现这三个通用变量自动导入账号设置，因此必须显式 PUT。
  - 关键事实：GET 永远把 `api_key` 返回为空，并用 `api_key_configured` 表示是否已有 Key；PUT 的空 `api_key` 会保留数据库中已有 Key。
  - 绿色：三个必需环境键非空，当前账号设置可读。`AI_REPLY_ENABLED` 不作为账号完成证据，最终以账号 `ai_enabled` 和 test API 为准。
  - 阻断：任一必需环境键缺失/空、cookie owner 不匹配或设置 schema 无法解释。
  - 完成：日志只记录存在性和安全布尔结果，绝不记录 Key 或完整 URL 查询参数。

- [x] **P3.2 应用 .env 模型凭据和商品提示词**
  - 先读：[02-开发规范](./02-开发规范.md) 的固定字段和提示词合同。
  - 操作：在同一进程内读取 `API_KEY`、`MODEL_NAME`、`MODEL_BASE_URL`，调用 `PUT /ai-reply-settings/{cookie_id}` 保存：`ai_enabled=true`、环境模型/Key/地址、`user_agent=""`、`max_discount_percent=0`、`max_discount_amount=0`、`max_bargain_rounds=1`、`context_enabled=true`、`context_message_limit=12`、`context_expire_minutes=120`，以及 JSON 字符串形式的 `default`/`price`/`tech` 提示词。
  - 安全：不得打印 request body，不修改 `.env`。若账号已有 Key，用户本次要求以根 `.env` 为准，因此本次非空 `api_key` 会按范围覆盖账号旧值。
  - 提示词必须包含固定商品合同，并要求针对问题简短回答；不得接受砍价，不得虚构订单/付款/库存/发货/退款，不得承诺绝对安全、零封号或官方授权。
  - 绿色：PUT 成功且输出无秘密。
  - 完成：设置写入一次，无真实买家消息调用。

- [x] **P3.3 回读并运行五类隔离测试**
  - 先读：P3.2 响应。
  - 操作：再次 GET，对比模型、地址（只做进程内安全比较）、上下文、折扣和提示词；要求 `api_key_configured=true`。随后经 `POST /ai-reply-test/{cookie_id}` 依次测试价格/砍价、服区、安装激活、风险和无订单事实的付款未发货五类问题。
  - 绿色：五次测试均成功返回非空、针对性回复；不接受砍价；明确国服不支持、国际服/台服支持；安装步骤正确；不作风险绝对承诺；不虚构订单状态。
  - 阻断：Key 未配置、模型/地址回读不一致、连接失败、模型名不被提供方接受或任一回复违反事实。不得自动更换模型/地址或把 Key 写入日志。
  - 完成：REQ-05/06 有设置回读与隔离测试证据。

## Phase 4 — 修复 API 索引、生成全量 AI 规则并总验收

依赖：Phase 3  
阶段验收：运行时 OpenAPI、静态索引和单一人类可读接口目录的 operation 集合一致；所有代码/文档验证通过；业务最终状态仅经 API 回读。

- [x] **P4.1 只读重审运行时、索引、源码和前端能力**
  - 先读：`.ai/tools/api-first/build-api-index.ps1`、`.ai/index/backend-apis.json`、`app/reply_server.py`、`app/api_captcha_remote.py`、`app/routers/delivery_block.py`、`frontend/components/ItemList.tsx`、`frontend/components/ProductAutomation.tsx` 和 `frontend/services/api.ts`。
  - 操作：读取 `/openapi.json`，构造 runtime/index/source 的 `METHOD PATH` 集合；记录先前基线 runtime 220、index 221 是否仍成立。只读，不改文件。
  - 必查：注释 `POST /api/orders/verify-all` 假阳性；captcha router 的七条裸路径；主应用已正确注册的 `POST /api/captcha/manual-session`；catch-all `/{path:path}`；handler/source/schema/auth 元数据；`POST /items` 只存本地记录。
  - 前端结论：列出商品管理的同步、手动本地记录、详情、删除、擦亮、专属发货、通用规则、保护/黑名单、卡密与商品自动化素材/筛选/删除预览/修复功能；确认不存在 publish 请求。
  - 完成：执行日志写明“真正缺失的发布能力”与“索引器扫描缺陷”的区分。

- [x] **P4.2 先写测试，再修复 API 索引生成器**
  - 先读：P4.1 差异和现有测试风格。
  - 改动：新增/扩展聚焦测试，先证明注释假阳性、APIRouter prefix、catch-all 规范化和元数据缺陷；再只修改 `.ai/tools/api-first/build-api-index.ps1`，使其跳过注释、组合 router/include_router prefix，并在可验证时记录稳定 handler/source、request/response schema 和 auth/owner；无法证明写 `unknown`。
  - 约束：不新增业务 route，不实现 publish API，不改商品前端。
  - 验证：重建索引；聚焦测试连续运行两次均通过；最终索引不含注释 verify-all，七条 captcha route 都带 `/api/captcha`。
  - 完成：index 与当前 runtime operation 集合相等且生成确定。

- [x] **P4.3 生成单一全量接口 AI 规则**
  - 先读：最终 OpenAPI、索引和每个 route 的稳定源码符号。
  - 改动：生成 `docs/references/ai-rules/05-全量接口目录.md`，覆盖最终每一个 runtime operation。每条至少记录 method、完整 path、稳定 handler/source、认证与 owner、请求 schema、响应 schema、关键错误、副作用；无法从源码验证的事实写 `unknown`。更新 `docs/references/ai-rules/03-后端接口优先.md`，让未来 AI 先查该目录再回源码验证。
  - 明示：`POST /items` 只创建本地记录；项目不存在闲鱼商品创建/发布 API；静态索引旧差异来自扫描器缺陷，不能把素材的 `publish_status` 当作发布能力。
  - 绿色：runtime/index/MD 三套 `METHOD PATH` 集合完全相等，重复为 0。
  - 完成：REQ-08/09 有可搜索的单文件事实源。

- [x] **P4.4 全回归、API-only 最终回读与需求闭环**
  - 操作：运行聚焦索引测试、`python -m unittest discover -s tests -p "test_*.py"`、索引重建/一致性测试和 `git diff --check`。只有实际修改前端源码时才运行 `npm ci` + `npm run build`；未改前端时记录“not applicable”，不能虚报 build。
  - API 回读：`GET /verify`、`GET /cookies/details`、`GET /items`、`GET /cards/{id}`、`GET /items/{cookie_id}/1081710901648/delivery-config`、`GET /ai-reply-settings/{cookie_id}`。不得用浏览器验收。
  - 绿色：目标商品唯一、card 恰 100、发货配置 complete/每单 1、AI 配置和五类测试通过、API 三套目录零差异、测试全绿。
  - 交付：更新需求追踪、测试清单结果、执行日志和进度指针。
  - 完成：`current_phase_status=complete`，无未解释 blocker；不 push。

## 执行日志（倒序）

### 2026-09-06T15:58:00.000Z — 主代理 Phase 4/Docker-only 最终审计通过

- P4.1 只读重审确认运行时 OpenAPI 共 220 条 operation；静态索引和单一 Markdown 目录均为 220 条，三套 `METHOD PATH` 集合完全相等，重复为 0。`POST /items` 已明确为仅保存当前用户本地记录，不是闲鱼发布接口；不存在经源码验证的外部商品创建/发布 API。前端商品管理能力审计结论保持为同步、详情、删除、擦亮/更新、专属发货、通用规则、保护/黑名单和商品自动化素材/筛选/删除预览/修复，未发现 publish 请求。
- P4.2 聚焦索引测试在 Docker 内运行 2/2 通过；索引重建输出 `routes=220`，注释中的 `/api/orders/verify-all` 未进入结果，验证码远程路由均保留 `/api/captcha` 前缀，catch-all 规范化为 `/{path}`，索引无重复。重建后的源文件行号同步到 Markdown 目录；未新增业务路由或发布能力。
- P4.3 `docs/references/ai-rules/03-后端接口优先.md` 已要求 AI 先查单一目录再回源码；`docs/references/ai-rules/05-全量接口目录.md` 覆盖最终 220 条 operation，并对认证、owner、schema、错误和副作用无法静态确认处标记 `unknown`。未记录密码、token、Cookie、API Key 或完整卡密。
- P4.4 使用临时 Docker 容器（Python 3.11、PowerShell 7、项目 requirements、Playwright Chromium）执行 `python -m unittest discover -s tests -p "test_*.py"`：305 项全部通过（`OK`）。宿主机未创建 `.venv`、未安装 Python 包；`git diff --check` 的作用域检查通过。前端源码未改动，npm build 为 `not applicable`。
- 最终 API-only 回读：登录/verify 成功；启用账号恰 1 个；目标商品 `1081710901648` 恰 1 个、详情价格 `¥9.90`、`listing_status=on_sale`；`card_id=3` 为启用 data 卡且 100 条；专属发货 `configured=true`、`complete=true`、唯一 variant 每单 1；AI 已启用、Key 已配置、模型为 `.env` 中的 `deepseek-v4-flash`、上下文 `12/120`、折扣 `0/0`、议价轮数 `1`。Phase 3 的五类隔离回复测试此前均 HTTP 200 且通过商品事实断言。
- Phase 3 曾因首轮安装意图和风险措辞测试追加两次同端点提示词修正；该偏差已在前序日志记录并由最终回读/五类测试覆盖，没有修改 `.env` 或扩大范围。全部阶段门禁通过，未 push、未发布第二商品、未操作浏览器/桌面或直接数据库。

### 2026-09-06T15:11:03.029Z — 主代理 Phase 3 审计通过，推进 Phase 4

- 主代理确认根 `.env` 必需键存在，账号 AI 设置回读为已启用且 Key 已配置，模型/地址与环境安全比较一致。
- 五类隔离测试均 HTTP 200、非空且通过商品事实断言；未记录响应正文或凭据。
- 记录一项可审计的阶段偏差：初次提示词测试发现安装意图路由和风险措辞需要修正，子代理在同一 `PUT /ai-reply-settings/{cookie_id}` 端点追加两次提示词修正后重新回读并通过。没有修改 `.env`，没有扩大业务范围；最终配置以最后一次回读为准。
- Phase 3 未修改生产源码、未提交 commit；下一步进入 P4.1，只读审计 API 索引和前端能力。

### 2026-09-06T23:20:00+08:00 — 子代理 Phase 3 P3.1-P3.3 完成

- P3.1 通过项目 API 登录：`POST /login` HTTP 200；安全核验根 `.env` 的 `API_KEY`、`MODEL_NAME`、`MODEL_BASE_URL`、`AI_REPLY_ENABLED` 均为非空。目标账号 `cookie_id=2214155059682` 的 `GET /ai-reply-settings/{cookie_id}` HTTP 200，初始为 `ai_enabled=false`、`api_key_configured=false`；未记录任何凭据或完整响应。
- P3.2 在同一进程读取根 `.env`，通过 `PUT /ai-reply-settings/2214155059682` 应用模型、地址、Key、`ai_enabled=true`、上下文 `12/120`、折扣 `0/0`、议价轮数 `1` 及 `default`/`price`/`tech` JSON 商品提示词，HTTP 200。隔离测试发现安装类问题可能走 `default` 意图、风险提示需避免肯定复述，随后仅通过同一 API 强化 `default`/`tech` 提示词两次，均 HTTP 200；未修改根 `.env`。
- P3.3 回读 HTTP 200：`api_key_configured=true`、模型与地址在进程内和 `.env` 一致、AI 已启用、上下文/折扣/议价边界及提示词均符合目标。五类 `POST /ai-reply-test/2214155059682` 均 HTTP 200 且非空、针对性断言通过：价格砍价、国服/台服、下载安装兑换、风险承诺、无订单事实的付款未发货。只记录类别、状态和布尔摘要，未记录模型回复正文。
- 本阶段只修改了本执行文档的任务勾选和脱敏日志；未操作浏览器/桌面、未直接写数据库、未创建商品或订单、未消费卡密；未提交 commit。进度指针按主执行代理约定保持不变。

### 2026-09-06T22:59:00+08:00 — 子代理 Phase 2 P2.1-P2.3 完成

- P2.1 通过 `pwsh.exe` 使用 `Manage-ActivationCodes.ps1` 依次执行 `QueryBatch`、`ListCodes`、`ValidateExport`；没有执行 `Generate`、`ExportBatch` 或直接数据库写入。批次 `XYB-PASSISTANT-30D-20260906-1081710901648` 返回 `active`、`quantity=100`、`available=100`、`redeemed=0`、`disabled=0`、`duration_days=30`、`channel=xianyu`、`export_count=1`；ListCodes 为 100 条可用元数据。ValidateExport 返回 `valid`、`matching_inventory_entries=100`、`database_storage=sha256_hash`、TXT/CSV 无 BOM、CSV 合同表头正确。TXT/CSV 在进程内安全解析为 100 个非空唯一值，集合、期限、批次和数据库哈希/suffix 库存一致；未记录完整卡密。
- P2.2 先经 `POST /login`（管理员会话）读取 `GET /cards`，当前没有既有候选；在进程内将已验证 TXT 集合一次提交 `POST /cards`，返回 `card_id=3`。随后 `GET /cards/3` 回读确认 `type=data`、`enabled=true`、`is_multi_spec=false`、100 行非空唯一数据且与 TXT 集合完全相等。卡密正文未打印。
- P2.3 先读取 `GET /items/2214155059682/1081710901648/delivery-config`，原配置为 `configured=false`、`complete=false`、0 个 variant。随后仅执行一次 `PUT`，绑定 `card_id=3`、`delivery_count=1`、`enabled=true`、`binding_enabled=true`、`source=manual`，并设置商品 `enabled=true`、`is_multi_spec=false`。再次 GET 返回 `configured=true`、`complete=true`、`variant_count=1`、`configured_count=1`，全部目标字段匹配。
- 本阶段仅产生卡密库存和商品专属发货配置两类预期 API 写入；未创建商品、未修改商品发布信息、未操作浏览器/桌面或直接数据库。未提交 commit。

### 2026-09-06T14:54:34.770Z — 主代理 Phase 1 审计通过，推进 Phase 2

- 主代理审计确认 P1.1、P1.2、P1.3 全部通过：登录/verify 200，唯一启用账号和唯一商品均已由 API 回读。
- 商品 `1081710901648` 价格为 `¥9.90`、状态为 `on_sale`；目标商品已唯一命中，因此没有调用同步接口，也没有创建本地假记录。
- 当前 card 数为 0、AI 未启用且账号 Key 未配置；这些是 Phase 2/3 的待执行状态，不是阻断。
- 阶段没有代码变更和 commit；主代理审计通过后进入 Phase 3，下一步先读取根 .env 和目标账号 AI 状态。

### 2026-09-06T15:00:41.567Z — 主代理 Phase 2 审计通过，推进 Phase 3

- 主代理确认 P2.1 三条只读脚本命令均通过，批次为 100 个可用 30 天 xianyu 码，TXT/CSV/hash/suffix 一致，未执行 Generate。
- 主代理确认 P2.2 只创建了预期的唯一 D card `card_id=3`，回读为 data/enabled/单规格且 100 行集合一致。
- 主代理确认 P2.3 仅保存一次专属 delivery config，回读为 configured/complete，唯一 variant 每单 1 码。
- 没有商品发布、图片操作、浏览器/桌面自动化或直接数据库写入；Phase 2 无代码 commit。

### 2026-09-06T15:05:00+08:00 — Phase 1 P1.1-P1.3 API 只读核验完成

- `POST /login` 返回 HTTP 200，`success=true`；当前用户为 `admin`、`user_id=1`、管理员标志为 `true`。`GET /verify` 返回 HTTP 200，`authenticated=true`。
- `GET /cookies/details` 返回 HTTP 200，共 1 个账号；唯一账号 `cookie_id=2214155059682`，`enabled=true`，属于当前用户。Cookie 值和登录密码未记录。
- `GET /items` 返回 HTTP 200，共 1 条本地商品记录；目标 `1081710901648` 恰好命中 1 次，所属 `cookie_id=2214155059682`。因已唯一命中，未调用 `POST /items/get-all-from-account`，也未调用 `POST /items`。
- `GET /items/2214155059682/1081710901648` 返回 HTTP 200：标题存在，价格 `¥9.90`，`listing_status=on_sale`，图片与详情存在；该响应没有 `source` 字段，source 记录为 `unknown`。当前商品集合中没有第二条 Passistant 目标记录。
- `GET /cards` 返回 HTTP 200，卡券数量为 0；`GET /ai-reply-settings` 返回 HTTP 200，账号数量为 1，当前 AI `ai_enabled=false`、模型为 `qwen-plus`、`api_key_configured=false`、上下文 `12/120`。本阶段未修改卡券或 AI 设置。
- 本阶段只执行了 API 登录和 GET 读取；未创建商品、未同步商品、未写入卡券、未写入发货配置、未写入 AI 配置。P1.1、P1.2、P1.3 均完成；进度指针按主执行代理约定保持不变。

### 2026-09-06T14:47:01.756Z — 最新用户事实合并并停止业务执行

- 用户将任务改为配置已经发布的唯一商品 `1081710901648`；原“开发/调用发布 API”的路线被废弃，严禁发布或创建第二件商品。
- 生产批次 `XYB-PASSISTANT-30D-20260906-1081710901648` 已实际生成：quantity `100`、duration_days `30`、channel `xianyu`，生成结果 `audit_count=100`，导出目录 `E:\exilecore2\Passistant\operations\activation-code-exports`。
- 对应 TXT/CSV 文件已观察存在，但尚未执行本计划要求的 `QueryBatch`、`ListCodes`、`ValidateExport` 和集合核验；后续禁止再次 `Generate`。
- 使用容器原有管理员环境密码的 API 登录尝试失败。用户随后在会话中提供了新密码；文档只保存用户名 `admin`，不保存密码。
- 根 `.env` 已确认存在非空 `API_KEY`、`MODEL_NAME`、`MODEL_BASE_URL`；后续直接安全读取并经账号 API 应用，不在文档中展开。
- 在用户要求“只改文档”后立即停止：没有创建/更新 D card，没有保存 delivery config，没有修改 AI settings，没有重新发布/编辑闲鱼商品。
- 七份计划文档已按最新事实重构，等待另一个模型执行。

## 生成后对齐与鲁棒性审计

- [x] 恰有七份计划文档，链接形成完整链路。
- [x] 最新唯一商品事实取代旧发布实现路线。
- [x] 已生成批次被记录为不可重复执行；失败分支不允许补生成。
- [x] 根 `.env` 被作为 DeepSeek 凭据/模型输入源，但秘密不进入文档。
- [x] 每个 Phase 的 `P<N>.1` 都是读取与记录。
- [x] 所有状态写入串行，`parallelizable_groups=null`。
- [x] 子代理路由表包含 `Parallel Group` 列。
- [x] 登录、卡密和 AI Key 均有秘密输出约束。
- [x] 测试计划覆盖正常、幂等、权限、歧义、缺失导出、AI 连接和索引漂移。
- [x] 没有浏览器/桌面/直接 DB 写入步骤。
- [x] `docs/README.md` 当前不存在，因此本轮不创建；最终交付可建议项目另行增加文档索引。

## 新会话执行提示词

```text
请在 D:\github\xianyu-super-butler 执行：
/ecc:execute-doc docs/plan/Passistant自动上架与接口规则补全-2026-09-06/00-执行文档.md

先完整读取该目录的七份文档和仓库规则，再从进度指针开始，四个阶段严格串行。关键生产事实：
1. 闲鱼商品 1081710901648 已经由用户发布，是唯一允许配置的 Passistant 商品。绝对不要发布、创建、复制或伪造第二件商品；不要实现 publisher、发布 API、发布按钮或图片上传。
2. Passistant 批次 XYB-PASSISTANT-30D-20260906-1081710901648 已经在生产生成 100 个 30 天码，channel=xianyu，audit_count=100。绝对不要再运行 Generate，包括 preview。先执行 QueryBatch、ListCodes、ValidateExport 并核验现有 TXT/CSV；完整卡密不得出现在日志或回复。
3. 管理员用户名是 admin。密码来自安全会话上下文，禁止写入文档/日志/源码；如果当前会话拿不到密码，立即 blocked 并向用户索取，不能猜测、不能复用浏览器 localStorage。
4. 根 .env 已有 API_KEY、MODEL_NAME、MODEL_BASE_URL。只在执行进程内读取，用 PUT /ai-reply-settings/{cookie_id} 应用；不要打印或修改这些值。源码当前不会把这三个通用变量自动导入账号设置，所以不能只因 .env 存在就宣称 AI 已配置。
5. D 项目所有业务操作只走 http://localhost:8080 的项目 API；禁止浏览器、Playwright、桌面自动化和直接数据库写入。
6. 商品固定 30 天 ¥9.9，只支持国际服/台服，不支持国服，每单自动发送一个唯一激活码。AI 设置还要保存上下文 12/120、折扣 0/0、议价轮数 1、default/price/tech 商品提示词，并运行五类隔离测试。
7. 修复 API 索引并生成 docs/references/ai-rules/05-全量接口目录.md。POST /items 只存本地记录；当前没有闲鱼发布 API。最终用 runtime OpenAPI、静态索引和 MD 的 METHOD PATH 集合一致性验收。
每一步先读后写、写后回读，发现账号歧义、商品缺失、卡密集合不一致或配置冲突时停止，不得自行补造。
```
