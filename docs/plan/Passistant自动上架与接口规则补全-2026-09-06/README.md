# Passistant 已发布商品配置与接口规则补全计划

## 任务背景

闲鱼商品已经由用户手动发布，唯一目标商品 ID 为 `1081710901648`。后续任务不再开发或调用闲鱼发布能力，而是通过 `<repo-root>` 已有 API 完成商品同步确认、100 个 30 天激活码库存导入、单规格自动发货绑定、DeepSeek 自动回复配置，以及全量 API AI 规则补全。

本目录用于交给另一个模型一次执行。推荐从 [00-执行文档](./00-执行文档.md) 开始，并使用 `/ecc:execute-doc` 按阶段执行。

## 已证实的现状

- 商品 `1081710901648` 已由用户发布，是唯一允许配置的 Passistant 商品；严禁发布、创建或复制第二件商品。
- 商品管理前端支持同步已有闲鱼商品、添加本地记录、编辑详情、删除本地记录、擦亮、卡密库存和专属/通用自动发货配置；没有“向闲鱼创建新商品”的 API。
- `POST /items` 只保存本地商品记录，不会发布到闲鱼。若同步后仍找不到目标商品，不得用该接口伪造归属，必须停止并报告。
- 已确认的相关 API 包括 `POST /login`、`GET /verify`、`GET /cookies/details`、`GET /items`、`POST /items/get-all-from-account`、`GET/POST /cards`、`GET /cards/{id}`、`GET/PUT /items/{cookie_id}/{item_id}/delivery-config`、`GET/PUT /ai-reply-settings/{cookie_id}` 和 `POST /ai-reply-test/{cookie_id}`。
- 静态 API 索引当前记录 221 条，先前运行时 OpenAPI 观察为 220 条。已定位一条注释 decorator 假阳性、七条验证码 router prefix 丢失和 catch-all 规范化问题；这不是发布接口“没扫描到”。
- Passistant 生产批次 `XYB-PASSISTANT-30D-20260906-1081710901648` 已生成 100 个、有效期 30 天、渠道 `xianyu` 的激活码，生成结果报告 `audit_count=100`。这是不可逆的既成事实，后续不得再次运行 `Generate`。
- 对应 TXT/CSV 文件位于 `E:\exilecore2\Passistant\operations\activation-code-exports`。后续必须先用受控脚本验证数据库和导出，再决定是否导入卡密库存。
- D 仓库根目录 `.env` 已有非空 `API_KEY`、`MODEL_NAME` 和 `MODEL_BASE_URL`。后续模型直接在进程内读取并应用，不能把值写入文档或输出。源码审计未发现这些三个通用变量会自动导入账号 AI 设置，所以仍需调用现有 AI 设置 API。
- 先前使用容器现有管理员环境配置调用 API 登录失败，说明其中密码已过期。用户随后在会话中提供了管理员密码；文档不保存密码。
- 尚未完成：目标账号与商品归属确认、导出核验、D 项目卡密创建、发货绑定、DeepSeek 账号设置写入/测试、API 索引修复和全量接口目录生成。
- 本轮仅修订文档，没有继续执行任何业务写入。

## 固定商品合同

| 字段 | 固定值/约束 |
|---|---|
| 商品 ID | `1081710901648` |
| 商品数量 | 仅此一件，不得新增第二件 |
| 产品 | Passistant，Path of Exile 2 的 Windows x64 叠加层辅助软件 |
| 规格 | 30 天月卡 |
| 价格 | ¥9.9 |
| 服区 | 支持国际服、台服；不支持国服 |
| 发货 | 确认付款后每单发送 1 个唯一激活码 |
| 下载与激活 | `https://passistant.vip`；以管理员方式运行 `Launcher.exe`；注册/登录后在个人中心兑换 |
| 表述边界 | 不虚构订单、付款、库存、发货或退款状态；不承诺绝对安全、零封号或官方授权 |

图片只保留为历史审计证据，不是后续执行步骤：

- 用户指定主图：`C:\Users\long\AppData\Local\Temp\codex-clipboard-5be824c3-f99c-48a1-bcaa-fd30c123a2ef.jpg`，SHA-256 `33aae217f92b5db4a45b651b89e4c4735887a74f1a87b0e54e997d5a0ff1ef48`。
- 当日 ¥9.9 图：`E:\exilecore2\Passistant\operations\taobao-listing\passistant-20260801\assets\passistant-main-image-prices-v6-monthly-3off-trial.png`，SHA-256 `be9f891567d81ec56391405e69fc9b91098ddff21a39cdba0667e84d685a1f3b`。

后续模型不得通过 API 上传、编辑或发布这些图片；商品发布已经由用户完成，当前项目也没有外部商品编辑/发布 API。

## 范围

本计划包含：

1. 使用 `admin` 账号经 `POST /login` 登录，并通过 API 唯一确定商品所属 `cookie_id`。
2. 只同步和核验商品 `1081710901648`，不创建第二件商品。
3. 核验已经生成的 100 个激活码批次和 TXT/CSV，再幂等创建或复用一个 D 项目卡密库存。
4. 为唯一商品绑定单规格、每单 1 码的自动发货配置。
5. 从根目录 `.env` 读取 DeepSeek Key、模型与地址，通过账号 API 保存商品回复配置并做隔离测试。
6. 修复 API 索引生成器，并生成 `docs/references/ai-rules/05-全量接口目录.md`。
7. 运行聚焦测试、Python 全套、索引一致性和最终 API-only 回读。

本计划不包含：

- 闲鱼商品发布、复制、重发、编辑或下架。
- 新增 publisher、发布任务状态机、素材创建/发布 API 或前端发布按钮。
- 浏览器、Playwright、桌面自动化、开发者工具注入或直接修改 D 项目运行数据库。
- 再次生成激活码。
- 修改或打印 `.env` 中的 DeepSeek 配置。
- Git push、PR、tag、第三方部署或 Passistant 正式发版。

## 文档索引

1. [00-执行文档](./00-执行文档.md)：唯一进度事实源、逐步任务、恢复协议和新会话提示词。
2. [01-架构设计](./01-架构设计.md)：API-only 配置链、能力边界、数据流和失败策略。
3. [02-开发规范](./02-开发规范.md)：接口、凭据、卡密、AI、目录生成和测试约束。
4. [03-修复路线图](./03-修复路线图.md)：四阶段串行路线、门禁与回退。
5. [04-测试计划](./04-测试计划.md)：验证层级、命令、PASS/BLOCKED/not verified 规则。
6. [05-测试用例清单](./05-测试用例清单.md)：54 个可填写证据的验收用例。

## 当前状态

| 项目 | 状态 |
|---|---|
| 商品已由用户发布 | 已完成，ID `1081710901648` |
| 100 个生产月卡生成 | 已完成，不得重做 |
| DeepSeek 配置写入根 `.env` | 已完成，值不在文档中展开 |
| 生产批次/导出核验 | 待执行 |
| 商品同步与账号唯一归属 | 待执行 |
| 卡密库存导入 | 待执行 |
| 自动发货绑定 | 待执行 |
| DeepSeek 账号设置与隔离测试 | 待执行 |
| API 索引修复与全量接口规则 | 待执行 |

## 最终审计结果

`PASS（仅指本计划文档质量）`

- 七份文档已按“既有唯一商品 + 已生成批次 + 根 `.env` 已配置 DeepSeek”的最新事实统一。
- 所有发布实现、发布 API、图片上传和第二商品步骤已移除。
- 每个阶段首项都是只读读取与记录，所有业务写入严格串行。
- 生产批次被标记为禁止再次生成，失败分支符合 Passistant 卡密技能的查询、预览和精确禁用合同。
- 密码、token、Cookie、API Key 和完整激活码均未写入文档。
- 业务配置当前仍为 `not started`；不得把文档审计 PASS 误报为自动发货或 AI 已生效。
