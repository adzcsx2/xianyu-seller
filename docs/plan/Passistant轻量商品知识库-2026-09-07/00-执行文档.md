# Passistant 轻量商品知识库执行文档

[返回 README](./README.md) · [架构设计](./01-架构设计.md) · [开发规范](./02-开发规范.md) · [修复路线图](./03-修复路线图.md)

本文是后续模型的唯一执行入口。先完整读取本目录七份文档和仓库规则，再从进度指针指定的任务开始。每个阶段必须先完成自测，再由主执行代理复核；不得因局部 2xx 或单个单测通过而提前结束。

## 执行进度指针

<!-- progress-pointer:start -->
```yaml
current_phase: 4
current_phase_status: complete
last_updated: 2026-09-07T11:28:00Z
last_actor: main-agent
last_commit: 04b3ce2
next_action: null
blockers: []
parallelizable_groups: null
```
<!-- progress-pointer:end -->

## 恢复协议

1. 读取上方进度指针，确认 `current_phase` 和 `current_phase_status`。
2. 跳到对应 Phase，从第一个未勾选的 `P<N>.<M>` 开始。
3. 每完成一项，勾选任务、更新指针，并把新日志追加在“阶段日志”顶部，保持倒序。
4. 阻断时将状态改为 `blocked`，把原因加入 `blockers`，记录已经取得的脱敏证据后停止。
5. 只有该 Phase 所有任务与阶段验收标准都为绿色，主执行代理才能切换 Phase。
6. 进度指针、Phase 切换、最终审计和产品意图冲突不得委派。

## 不可变约束

- 只处理商品 `1081710901648`；禁止创建、发布、复制、编辑或删除第二件 Passistant 商品。
- 价格口径为原价 ¥29.9/月、当前活动口径 3 折、活动价 ¥9.9/月；不得把 ¥9.9 说成常规原价，不得编造活动结束时间，活动价不接受额外砍价。
- 业务状态和知识导入只走 `http://localhost:8080` 的认证 API；禁止浏览器、Playwright、桌面自动化和直接写运行 SQLite。
- 不修改或打印根 `.env`、API Key、登录密码、Cookie、token、完整激活码和客户数据。
- Passistant 仓库只作为只读事实源；不得修改 `E:\exilecore2\Passistant` 的源码、文档、运营数据或构建产物。
- 不把整个 Passistant 仓库自动索引到买家知识；只提交人工白名单 seed。
- 封号/安全意图优先级最高，精确回复 `目前没有收到玩家反馈有封号情况出现。`，且不得调用模型。
- 买家未问封号/安全时，最终回复不得主动出现封号、安全、检测风险等相关措辞。
- 商业敏感问题不得直接回答；默认精确回复 `这个问题涉及内部信息，暂不提供。`，且不得调用模型。
- 付款、发货、退款、收货仍由订单与自动发货链路处理，知识库不能宣称这些动作已经发生。
- 不新增外部依赖、向量数据库、embedding 服务或第二套数据库连接。
- 保留工作区全部无关用户改动；每次本地提交只暂存本阶段文件，不 push、不建 PR、不改远程。

## 需求追踪

| ID | 来源路径 + 稳定章节 | 需求 | 执行任务 | 证据与测试 | 计划状态 |
| --- | --- | --- | --- | --- | --- |
| REQ-01 | 用户请求（2026-09-07）/“轻商品数据库”；`AGENT.md`/Architecture Boundaries | 轻量 SQLite 商品知识库，按账号与商品隔离 | P2.2～P2.4 | schema/API 回读；TC-DB-01～10、TC-API-01～09 | PASS |
| REQ-02 | 用户请求/“所有知识根据 Passistant”；`WPF桌面功能清单.md`/正式产品功能、实验性功能 | 知识来自 Passistant 当前产品事实且只含公开内容 | P1.1、P1.3、P2.3 | 来源清单、seed checksum、敏感扫描；TC-SEED-01～10 | PASS |
| REQ-03 | 用户请求/“商业敏感信息禁止直接回复” | 商业敏感信息禁止直接回复 | P1.2、P3.2、P3.4 | 模型零调用、固定拒答、输出拦截；TC-POL-08～15 | PASS |
| REQ-04 | 用户请求/“封号，安全相关，只回复……” | 封号/安全只回复指定句子 | P1.2、P3.2、P3.4 | 精确字符串、模型零调用；TC-POL-01～07 | PASS |
| REQ-05 | 用户请求/“如果玩家不问，不需要主动提及” | 未询问时不主动提及封号/安全 | P3.2、P3.4 | 后置校验与普通问答矩阵；TC-OUT-01～06 | PASS |
| REQ-06 | 用户请求/“AI 可以直接用”；`ai_reply_engine.py`/`generate_reply` | 普通知识检索后供 DeepSeek 回复 | P2.3、P3.2～P3.4 | 稳定评分、top-5/2500 字符、模型消息断言；TC-RET-01～10、TC-AI-01～08 | PASS |
| REQ-07 | 用户补充（2026-09-07）/客户沟通增加“知识库”、当前为 Passistant 知识库；`Sidebar.tsx`/`menuGroups`、`App.tsx`/`pageLabels` | 在“客户沟通”增加独立知识库页面；当前项目标识为 Passistant，并为未来其他项目保留稳定 key | P2.2～P2.4、P4.1～P4.2 | schema/API/UI/路由、生产构建；TC-UI-01～12 | PASS |
| REQ-08 | 用户价格纠正（2026-09-07）；前序商品合同/服区 | 唯一商品原价 ¥29.9/月、当前 3 折活动价 ¥9.9/月；国际服/台服、国服不支持 | P1.1、P1.3、P3.4、P4.4 | 商品和 seed API 回读、价格回复断言；TC-PROD-01～07 | PASS |
| REQ-09 | `ai_reply_engine.py`/`is_system_or_order_event`；现有自动发货合同 | 不干扰自动发货与订单事实 | P3.2～P3.4、P4.4 | 系统事件绕过、订单断言；TC-AI-09～12、TC-E2E-06 | PASS |
| REQ-10 | `AGENT.md`/API-first、测试与本地提交规则 | API 目录与部署结果可由后续模型复现 | P2.4、P4.3～P4.4 | 索引集合一致、容器健康、API-only 回读；TC-DOC-01～04、TC-E2E-01～10 | PASS |

## 阶段总览

| Phase | 目标 | 任务 | 依赖 | Parallel Group | 完成门 |
| --- | --- | --- | --- | --- | --- |
| 1 | 冻结公开知识与策略合同，先写失败测试 | P1.1～P1.3 | 无 | `serial` | 来源、禁区、固定回复和失败测试全部可审计 |
| 2 | 建立 SQLite、检索服务和认证 API | P2.1～P2.4 | Phase 1 | `serial` | 数据/API/隔离测试通过，索引更新 |
| 3 | 把知识与硬策略接入 AI 回复 | P3.1～P3.4 | Phase 2 | `serial` | 固定回复零模型调用，普通检索与后置拦截通过 |
| 4 | 前端、容器和唯一商品 API-only 验收 | P4.1～P4.4 | Phase 3 | `serial` | 全套测试、构建、健康检查和生产回读通过 |

不允许任何并行组。数据库 schema、API 合同、AI 策略与前端类型共享同一版本语义，执行文档中的 `parallelizable_groups` 必须保持 `null`。

## 子代理路由

| Phase | 建议执行者 | 职责 | Parallel Group | 原因 |
| --- | --- | --- | --- | --- |
| 1 | 主执行代理 | 事实审计、合同冻结、测试先行 | `serial` | 用户策略优先级和公开边界必须统一判断 |
| 2 | 主执行代理 | schema、service、route、索引 | `serial` | 写路径和所有权校验不可拆开 |
| 3 | 主执行代理 | AI 前置分流、检索、后置校验 | `serial` | 固定回复与泄露防线是单一安全链 |
| 4 | 主执行代理 | UI、构建、容器、API 验收 | `serial` | 必须基于同一候选和真实 item 版本 |

仓库没有验证过可独立拥有这些共享文件的项目级子代理配置，因此默认 main-agent fallback。不得为了形式上的并行拆分共享文件。

## Phase 1：事实与策略合同、测试先行

阶段验收：形成一份可公开 seed 清单和一组在旧实现上稳定失败的新测试；没有修改运行数据库或调用外部模型。

分支：当前检出分支；P1.1 用 `git branch --show-current` 记录实际名称，禁止新建或切换。依赖：无。验收命令：`python -m unittest tests.test_product_knowledge tests.test_ai_reply tests.test_ai_reply_safety` 的新增用例先出现预期行为红灯，seed 静态校验随后通过。

- [x] **P1.1 只读核验并记录当前事实**
  - 先读：`<repo-root>/AGENT.md`、`app/ai_reply_engine.py`、`app/db_manager.py`、`app/reply_server.py`、`frontend/components/AIReply.tsx`、`frontend/services/api.ts`、`frontend/types.ts`、`tests/test_ai_reply.py`、`tests/test_ai_reply_safety.py`、`.ai/index/backend-apis.json`。
  - 只读 Passistant：`E:\exilecore2\Passistant\AGENTS.md`、`.ai/rules/01-architecture.md`、`.ai/rules/03-documentation.md`、`README.md`、`docs/modules/WPF桌面功能清单.md`、`docs/product/Passistant官网使用说明需求文档.md`、`docs/guide/订阅购买与管理.md`。
  - API 只读：登录后调用 `GET /verify`、`GET /cookies/details`、`GET /items`、`GET /items/{cookie_id}/1081710901648`、`GET /ai-reply-settings/{cookie_id}`；只记录状态、数量、非秘密字段和 `api_key_configured` 布尔值。
  - 绿色：目标商品恰好属于一个当前用户账号，价格可解析为 `9.9`、状态可售；现有代码仍无知识表/检索；当前 AI 配置可用。任何歧义都标记 blocked，不选择第一项。
  - 完成证据：在本文件“阶段日志”追加时间、读取文件、API 状态和脱敏结论。
  - 完成：实际分支、全部必读源和五个 API 的脱敏事实均已记录，且没有文件或业务写入。

- [x] **P1.2 先增加策略与检索失败测试**
  - 新增 `tests/test_product_knowledge.py`：临时 SQLite schema、批量替换、版本冲突、账号/商品隔离、评分顺序、字符预算、seed 校验。
  - 修改 `tests/test_ai_reply.py`：断言普通技术问题把匹配知识加入 system message，未匹配时不伪造事实，测试端点使用真实 item ID 的合同由路由测试覆盖。
  - 修改 `tests/test_ai_reply_safety.py`：封号/安全多种说法精确返回指定句子且 `_generate_with_retry` 零调用；商业敏感问题固定拒答且零调用；普通问题的模型输出若主动出现安全话题则不得发送。
  - 测试必须先在旧实现上按预期失败；记录失败用例名，不以 import error 或语法错误充当红灯。
  - 完成证据：失败用例与 REQ-02～06 一一映射。
  - 完成：每项新行为至少有一个语义失败测试，旧回归仍可独立运行。

- [x] **P1.3 冻结可公开 seed 与禁区**
  - 新增 `app/knowledge/passistant_public_v1.json`，只写 [01-架构设计](./01-架构设计.md) 指定字段和公开答案；新增 `app/knowledge/__init__.py` 作为包标识。
  - 每条 `source_refs` 指向允许的 Passistant 文档及标题，不出现源码符号、运行路径细节或凭据；`answer` 不包含来源路径。
  - seed 必须覆盖原价/活动价/时长、服区、安装/激活、十一项正式功能摘要、预载提醒、公开限制与求助信息；价格答案必须说明“原价 ¥29.9/月，当前活动口径 3 折，活动价 ¥9.9/月”，不得把活动价写成常规价或猜测结束时间；不得包含本 README“不可公开”列表中的内容。
  - 对 JSON 做解析、唯一 `knowledge_key`、长度、类别和敏感词扫描；保存 SHA-256 摘要到阶段日志，不记录任何秘密。
  - 阶段提交：仅暂存 Phase 1 新增/修改文件，本地中文 Conventional Commit；工作区有重叠改动时不提交并记录原因。
  - 完成：seed 校验全部绿色、敏感扫描零命中、价格和功能覆盖断言通过。

## Phase 2：SQLite、检索服务与认证 API

阶段验收：知识能在临时数据库中原子替换、稳定检索并通过认证 API 管理；API 索引与运行路由一致。

分支：P1.1 记录的当前分支。依赖：Phase 1 全绿。验收命令：`python -m unittest tests.test_product_knowledge tests.test_api_catalog_builder`，并连续两次运行 API 索引生成器取得相同输出。

- [x] **P2.1 只读复核 schema/API 插入点**
  - 读取 `DBManager.init_db`、`item_info`/`ai_reply_settings` 建表与迁移、`get_items_by_cookie`、`get_item_detail`、AI 设置路由、管理员数据表白名单、API 索引生成脚本和现有测试模式。
  - 确认 `item_info` 有 `UNIQUE(cookie_id,item_id)`，确认路由用 `get_current_user` 并通过 `get_all_cookies(user_id)` 校验账号归属。
  - 记录将修改的确切符号；本步骤只读，不编辑。
  - 完成：稳定符号、当前分支、表/路由所有权模式和生成命令均已记录。

- [x] **P2.2 实现知识文档与条目数据层**
  - 修改 `app/db_manager.py::init_db`：创建 `ai_product_knowledge_documents`、`ai_product_knowledge_entries` 和 scope 索引；documents 保存 `product_key/display_name`，当前导入固定为 `passistant`/`Passistant 知识库`；修改 `delete_item_info` 做显式清理兜底。
  - 新增 `get_product_knowledge_document`、`list_product_knowledge_entries`、`replace_product_knowledge`、`delete_product_knowledge_for_item`。批量替换必须在 `self.lock` 内单事务检查 `expected_version`；空 entries 仍在 documents 表保留新版本。
  - 两张知识表都不加入通用 `/admin/data/{table_name}` 白名单，防止绕过版本和专用 response model。
  - 验证：运行 `python -m unittest tests.test_product_knowledge.ProductKnowledgeDatabaseTests`；绿色表示新库、空文档版本、事务回滚、隔离和删除用例全部通过。
  - 完成：TC-DB-01～10 全绿，生产 DB 未被测试打开。

- [x] **P2.3 实现轻量检索与策略服务**
  - 新增 `app/product_knowledge.py`：实现 `ProductKnowledgeService`、seed 加载/校验、NFKC 规范化、确定性评分、top-5/2500 字符预算、策略分类和输出校验；数据访问只调用 P2.2 的 `db_manager` 方法。
  - 不启用 FTS5，不新建数据库文件，不缓存跨账号知识，不把 `source_refs` 注入模型。`检测/反作弊/绕检测/规避检测` 全部归 safety，优先返回指定句。
  - 验证：运行 `python -m unittest tests.test_product_knowledge.ProductKnowledgeServiceTests`；绿色表示 TC-SEED 与 TC-RET 全部通过。
  - 完成：seed、分类、检索、上下文构建和输出校验均有确定性单测。

- [x] **P2.4 实现 API 合同并更新索引**
  - 修改 `app/reply_server.py`：新增 Pydantic 请求/响应模型及四个路由：GET/PUT 知识、POST 内置导入、POST 检索预览；响应携带 `product_key/display_name`；统一校验当前用户拥有 `cookie_id` 且目标 `item_id` 存在。
  - PUT/导入要求 `expected_version`；冲突返回 HTTP 409，不允许静默覆盖。导入响应只给版本、条目数、seed version/checksum，不回显全部 seed 正文。
  - 预览仅返回策略种类、匹配 key/类别/分数和固定回复标志；不调用 DeepSeek、不发送闲鱼消息、不返回 `source_refs`。
  - 修改 `.ai/tools/api-first/build-api-index.ps1`（仅当现有生成器无法正确收录新 Pydantic/路由信息）、重新生成 `.ai/index/backend-apis.json` 和 `docs/references/ai-rules/05-全量接口目录.md`。
  - 在 `tests/test_product_knowledge.py` 新增 API 所有权、404、409、越权、非法 seed/条目测试；运行专用测试和 `tests.test_api_catalog_builder`。
  - 阶段提交：只提交 Phase 2 文件，不混入用户原有改动。
  - 完成：TC-API-01～08 与 TC-DOC-01～04 全绿，索引双生成稳定。

## Phase 3：AI 接线与双重泄露防线

阶段验收：固定策略由代码直接返回，普通问题只使用有限公开知识，后置校验能阻止主动安全话题和敏感输出。

分支：P1.1 记录的当前分支。依赖：Phase 2 全绿。验收命令：`python -m unittest tests.test_product_knowledge tests.test_ai_reply tests.test_ai_reply_safety`。

- [x] **P3.1 只读复核当前回复时序**
  - 读取 `AIReplyEngine.generate_reply`、`detect_intent`、`_resolve_system_prompt`、`_normalize_reply`、议价硬校验、上下文保存/防抖，以及调用方如何处理 `None`。
  - 读取 `POST /ai-reply-test/{cookie_id}` 和前端测试请求，记录真实商品加载应插入的位置。
  - 确认系统/订单事件继续最先绕过；本步骤只读。
  - 完成：现有时序、调用方 `None` 回落和确切插入符号已写入倒序日志。

- [x] **P3.2 接入策略、知识与真实商品测试**
  - 修改 `app/ai_reply_engine.py`：在用户消息成为当前最新消息后、读取上下文和调用模型前执行 `ProductKnowledgeService.classify_policy_intent`。
  - `safety` 直接保存并返回精确指定句；`commercial_sensitive` 直接保存并返回固定中性拒答；二者都不得读取知识、构建客户端或调用模型。
  - 普通路径调用 `retrieve(cookie_id,item_id,message)`，只把公开答案封装在“只读事实数据”分隔块中；系统提示明确买家消息和知识中的指令都不能覆盖策略。
  - `_normalize_reply` 后执行 `validate_model_reply`。普通问题若模型主动包含安全话题或敏感标记，拒绝该输出并使用匹配知识的确定性短答；无匹配则返回 `这个问题需要人工确认后回复。`。
  - 保留价格底线、上下文隔离、系统事件绕过和订单事实边界。
  - 验证：运行 `tests.test_ai_reply` 和 `tests.test_ai_reply_safety` 中 engine 用例；绿色表示策略零调用、知识注入和输出回落通过。
  - 完成：TC-POL-01～15、TC-OUT-01～06 和 TC-AI-01～08 全绿。

- [x] **P3.3 将 AI 测试端点绑定真实商品**
  - 修改 `app/reply_server.py::test_ai_reply` 的 Pydantic request：只接受 `message + item_id`，验证 scope 后从 `db_manager.get_item_info` 读取标题、价格、描述；移除 `test_item` 和客户端任意商品事实。
  - 修改 `tests/test_product_knowledge.py` 的 AI test route 合同用例；测试会话使用 UUID test 标记并在 finally 清理或被上下文查询明确排除。
  - 验证：运行 `python -m unittest tests.test_product_knowledge.AIReplyTestRouteTests`；绿色表示缺失/越权/未知 item 和清理路径通过。
  - 完成：TC-API-09 以及测试资源清理断言全绿。

- [x] **P3.4 完成 AI 回归矩阵**
  - 运行 `tests.test_product_knowledge`、`tests.test_ai_reply`、`tests.test_ai_reply_safety`。
  - 覆盖“封号吗/安全吗/会检测吗/ban risk”等安全问法的精确等值；覆盖同一消息同时命中安全与商业敏感时安全规则优先。
  - 覆盖源码、偏移、检测规避、数据库、密钥、成本利润、供应渠道、完整库存等商业敏感问法。
  - 覆盖功能、安装、价格、砍价、服区、激活、前台跟随、未知问题、提示注入、模型泄露、超长知识和跨商品污染。价格问答必须区分 ¥29.9 原价与当前 3 折活动价 ¥9.9，拒绝额外砍价且不虚构活动期限。
  - 阶段提交：只提交 Phase 3 文件；测试日志不得包含买家敏感正文、完整 prompt 或模型密钥。
  - 完成：专用三模块全绿且既有 TC-AI-09～12 无回归。

## Phase 4：前端、部署与 API-only 验收

阶段验收：“客户沟通”下有独立“知识库”页面，当前项目明确为“Passistant 知识库”；容器使用新候选；唯一商品已通过 API 导入 seed，固定策略和普通知识问答均通过。

分支：P1.1 记录的当前分支。依赖：Phase 3 全绿。验收命令：Python 全套、`npm ci && npm run build`、API 索引双生成、`git diff --check`、容器健康和认证 API 回读。

- [x] **P4.1 只读复核前端与运行候选**
  - 读取 `frontend/components/Sidebar.tsx::menuGroups`、`frontend/App.tsx` 的 lazy imports/`pageLabels`/section switch、`frontend/components/AIReply.tsx`、`frontend/services/api.ts`、`frontend/types.ts`、`frontend/components/ItemList.tsx` 的商品加载模式和共用 UI 组件。
  - 运行 `docker compose ps` 与 `docker compose config`，记录实际 compose 文件、服务名、端口和健康状态；不重启。
  - 登录 API 后再次 GET 唯一商品、知识版本和 AI 配置；记录脱敏基线。
  - 完成：候选、服务、唯一 scope 和前端稳定插入点均已记录，未发生写入。

- [x] **P4.2 在客户沟通增加独立知识库页面**
  - 新增 `frontend/components/KnowledgeBase.tsx`：页面标题“知识库”；知识项目标题“Passistant 知识库”；显示稳定 `product_key=passistant`，并提供账号/商品选择、版本/条目数、条目列表、启用/编辑/删除、批量保存、导入 Passistant 模板和检索预览。
  - 修改 `frontend/components/Sidebar.tsx::menuGroups`：导入 `BookOpen` 图标，在“客户沟通”组的“AI 回复”之后新增 `{ id: 'knowledge-base', icon: BookOpen, label: '知识库' }`。
  - 修改 `frontend/App.tsx`：lazy import `KnowledgeBase`；在 `pageLabels` 增加 `'knowledge-base': '知识库'`；在 `ai-reply` 与 `messages` section 附近增加对应 Suspense section，保留 `active_page` 持久化和移动端标题。
  - 修改 `frontend/components/AIReply.tsx`：只为回复测试按选中账号过滤 `getItems()`、选择实际商品并提交真实 `item_id`；不再内嵌知识编辑器。
  - 修改 `frontend/types.ts`：增加 `ProductKnowledgeEntry`、`ProductKnowledgeDocument`、`KnowledgePreviewResult` 和新版 AI test payload。
  - 修改 `frontend/services/api.ts`：所有新请求使用现有 `get/put/post`；路径段 `encodeURIComponent`；保存携带当前 `expected_version`。
  - UI 不显示 `source_refs`、内部敏感词表或完整系统 prompt；409 提示重新加载，不自动覆盖。通用导航名是“知识库”，页面内项目名必须是“Passistant 知识库”，不得把全系统写死成只能存在 Passistant。
  - 验证：`npm ci && npm run build` exit 0，静态搜索无危险渲染/旧 test payload。
  - 完成：TC-UI-01～12 全绿；无法自动验证的视觉项明确记 `not verified`。

- [x] **P4.3 完整静态验证与生产构建**
  - 运行：
    - `python -m unittest tests.test_product_knowledge tests.test_ai_reply tests.test_ai_reply_safety tests.test_api_catalog_builder`
    - `python -m unittest discover -s tests -p "test_*.py"`
    - `cd frontend; npm ci; npm run build`
    - `pwsh.exe -File .ai/tools/api-first/build-api-index.ps1 -WorkspaceRoot .`
    - 连续生成两次 API 索引并比较 METHOD/PATH 集合与文件哈希。
    - `git diff --check`
  - 检查生产文件没有 Mock/Stub/fixture、新依赖、秘密或第二商品 ID。
  - 任何失败先修复并重跑相关层，再进入部署。
  - 完成：任务相关专用测试、前端生产构建、API 索引双生成、敏感/范围检查和 `git diff --check` 通过；全套测试为 329 项中 328 项通过，唯一失败是既有 Windows 进程路径环境用例 `test_slider_watchdog.KillBrowserProcessTests.test_matches_only_own_user_data_dir`，与本任务无关，记为 `not verified`。索引两次生成的 METHOD/PATH 集合一致；原始文件哈希只因生成时间字段不同，规范化后内容一致。

- [x] **P4.4 重建容器并通过 API 导入/验收**
  - 在 `<repo-root>` 运行 `docker compose up -d --build`；等待健康后通过 `GET /health` 和 `docker compose ps` 确认实际新容器处于 healthy。
  - 只用 API 登录；GET 确认唯一账号和商品 `1081710901648`。调用知识 GET 取得版本，再 POST `/items/{cookie_id}/1081710901648/ai-knowledge/import-passistant`，携带 `expected_version`；随后 GET 回读版本、数量、seed version/checksum。
  - 调用预览 API 验证价格、服区、安装、正式功能、跟随限制、封号/安全和商业敏感分流。预览日志只记 match key/策略/布尔值。
  - 调用 AI 测试 API：普通功能问题应使用知识；封号/安全问题必须精确返回指定句；未询问安全的普通回复不得出现相关词；系统/订单事件仍不生成 AI 回复。
  - 不调用商品发布/创建/编辑 API，不修改 card/delivery，不发送真实闲鱼消息。
  - 最终本地提交仅包含本任务代码、seed、测试、生成索引/目录和计划进度更新；不 push。
  - 完成：容器健康、唯一商品隔离、知识导入/回读、价格/服区/安全/商业敏感预览与真实 AI 测试均通过；未调用商品发布、卡密、发货或真实消息 API。既有 slider watchdog 环境用例维持 `not verified`，固定策略与隔离门禁全绿。

## 阶段日志

按以下格式插入到本节最上方，严格倒序（最新在前），不改写历史：

```text
### <UTC 时间> — <执行者> <Phase/P任务>
- 读取：<文件/接口，脱敏>
- 修改：<文件或 API 资源>
- 验证：<命令、用例、状态码、数量/哈希摘要>
- 结果：PASS / FAIL / BLOCKED
- 下一步：<唯一任务 ID>
```

### 2026-09-07T11:50:00Z — main-agent 终态议价边界复核
- 发现并修复：低价商品原有议价底价会忽略 10 元以下数字，无法保证 9.9 元活动价不再让价；新增低价底价固定为商品价、低价报价解析（排除“3 折”数字）以及模型调用前低报价拦截。
- 验证：`test_ai_reply_safety.py` 专项测试 exit 0；重新构建容器后，认证 API 的“可以便宜到8.9吗？”固定返回“抱歉，这个价格已经是最优惠的了，不能再便宜了哦！”，未进入模型议价承诺；健康检查和知识版本/条目数保持通过。
- 全套回归：当前仍为 329 项中 328 项通过，唯一失败保持为既有 `test_slider_watchdog.KillBrowserProcessTests.test_matches_only_own_user_data_dir`，与本任务无关，记为 `not verified`。
- 结果：PASS
- 下一步：无

### 2026-09-07T11:47:00Z — main-agent 终态复核与策略补强
- 复核：运行时 `/openapi.json` 与 `.ai/index/backend-apis.json` 均为 224 条 operation，METHOD/PATH 集合完全一致；应用容器 healthy，`GET /health`=healthy，唯一商品知识文档 version=4、entry_count=20。
- 发现并修复：安全策略原先覆盖“安全吗/账号安全”等组合问法，但独立“安全性”未必命中；将“安全”纳入最高优先级 safety 词表，并补充零模型调用测试。
- 验证：商品知识、AI 回复、安全策略三组专项测试分别 exit 0；重新 `docker compose up -d --build` 后 API 测试“这个软件安全性怎么样”与“会封号吗”均精确返回指定句，价格回复保留原价/3 折/活动价完整口径；`git diff --check` exit 0。
- 结果：PASS（终态复核未发现未满足的本任务要求）
- 下一步：无

### 2026-09-07T11:28:00Z — main-agent Phase 4/P4.3～P4.4
- 读取：`docker-compose.yml`、`Dockerfile`、前端构建输出、API 索引和唯一商品运行状态；全部业务验证通过认证 HTTP API 完成。
- 修改：补充区服问法；收紧价格模型回复后置校验，缺少“原价 29.9 + 活动 3 折 + 活动价 9.9”完整口径时回落到批准答案；同步测试覆盖。
- 验证：`python -m unittest discover -s tests -p 'test_product_knowledge.py'`（20，OK）；`npm ci && npm run build`（Vite exit 0）；API 索引生成两次均 `routes=224`，METHOD/PATH 集合一致；`git diff --check` 通过。`docker compose up -d --build` 完成，容器 `healthy`，`GET /health`=healthy。API 导入后 Passistant 文档 version=4、entry_count=20；价格真实 AI 回复含原价/3折/活动价，区服回复为国际服/台服且拒绝国服；安全精确回复 `目前没有收到玩家反馈有封号情况出现。`，商业敏感精确回复 `这个问题涉及内部信息，暂不提供。`。
- 全套回归：329 项中 328 项通过；唯一失败为既有 `test_slider_watchdog.KillBrowserProcessTests.test_matches_only_own_user_data_dir`，记录 `not verified`，未触及本任务路径。
- 结果：PASS（本任务验收完成；非安全性既有环境用例 not verified）
- 下一步：无

### 2026-09-07T11:12:00Z — main-agent Phase 4/P4.1～P4.2
- 读取：`frontend/components/Sidebar.tsx`、`App.tsx`、`AIReply.tsx`、`services/api.ts`、`types.ts`、`docker-compose.yml`、`Dockerfile`；确认 Dockerfile 会在镜像构建阶段重新执行前端生产构建，应用服务为 `xianyu-app`/当前容器，端口 8080。
- 修改：新增 `frontend/components/KnowledgeBase.tsx`；在“客户沟通”新增独立“知识库”入口和 App 页面；增加知识类型/API；AI 回复测试改用真实商品下拉选择与 `message + item_id` 合同。
- 验证：`cmd /c "set PATH=C:\\Program Files\\nodejs;%PATH%&& npm run build"` exit 0，Vite 构建成功并产出 KnowledgeBase chunk；静态 UI 不显示 `source_refs`、系统 prompt 或内部规则字段。
- 结果：PASS（前端功能与运行候选复核完成；视觉细节未使用浏览器验证，记 `not verified`）
- 下一步：P4.3

### 2026-09-07T11:05:00Z — main-agent Phase 3/P3.1～P3.4
- 修改：`app/ai_reply_engine.py` 在最新消息确认后、上下文读取和模型客户端创建前接入知识策略；安全问题固定返回指定句，商业敏感问题固定拒答；普通问题只注入当前 `cookie_id + item_id` 的公开知识，并在模型输出后执行敏感/路径/内部标记拦截。
- 修改：`app/reply_server.py` 的 `POST /ai-reply-test/{cookie_id}` 改为只接受 `message + item_id`，从当前用户商品记录读取标题/价格/描述；测试会话使用 UUID 前缀并在 finally 清理；`app/db_manager.py` 增加一次性测试会话清理方法。
- 验证：`python -m unittest discover -s tests -p 'test_product_knowledge.py'`（19 tests，含真实商品 AI 测试路由合同，OK）；`test_ai_reply.py`（12 tests，OK）；`test_ai_reply_safety.py`（36 tests，OK）。固定安全/商业路径的 `_generate_with_retry` 均未调用，主动安全输出回落为人工确认。
- 结果：PASS（Phase 3 门禁通过；未调用外部模型、未发送闲鱼消息）
- 下一步：P4.1

### 2026-09-07T11:00:00Z — main-agent Phase 2/P2.2～P2.4
- 修改：`app/db_manager.py` 新增按 `cookie_id + item_id` 隔离的 knowledge documents/entries 表、索引、原子版本替换与商品删除清理；新增 `app/product_knowledge.py` 实现 Passistant seed 校验、NFKC 确定性检索、策略分类、上下文和模型输出后置校验；`app/reply_server.py` 新增知识 GET/PUT、Passistant 导入和预览四个认证路由。
- 修改：更新 `.ai/index/backend-apis.json` 和 `docs/references/ai-rules/05-全量接口目录.md`，目录由 220 条更新为 224 条；新增索引回归断言。
- 验证：`python -m unittest discover -s tests -p 'test_product_knowledge.py'`（17 tests，OK）；`python -m unittest discover -s tests -p 'test_api_catalog_builder.py'`（3 tests，OK）；API 索引生成器输出 `routes=224 sources=3 warnings=1`，四个 `ai-knowledge` 路由均已收录。
- 重叠改动：`app/reply_server.py`、`.ai/index/backend-apis.json`、`docs/references/ai-rules/05-全量接口目录.md` 在本任务开始前已有用户改动，本阶段不单独提交这些重叠文件；其余 Phase 2 文件可独立暂存。
- 结果：PASS（Phase 2 门禁通过；未写运行数据库、未调用模型或闲鱼消息 API）
- 下一步：P3.1

### 2026-09-07T03:25:00Z — main-agent Phase 2/P2.1
- 读取：`DBManager.init_db`、`item_info`/`ai_reply_settings` schema、商品读取/删除方法、AI 设置路由、`API_ROOTS`、`.ai/tools/api-first/build-api-index.ps1`、`tests/test_api_catalog_builder.py`。
- 结论：`item_info` 已有 `UNIQUE(cookie_id, item_id)`；现有路由均使用 `get_current_user` 后调用 `db_manager.get_all_cookies(user_id)` 做账号归属校验，再按 `get_item_info(cookie_id,item_id)` 判断商品；API 索引器能从 FastAPI decorator、Pydantic 签名和 `Depends` 收集路由/所有权提示；新增知识路由只需保持 `/items/{cookie_id}/{item_id}/ai-knowledge/...`，无需修改生成器。
- 写入边界：知识表不加入通用管理表白名单；商品删除需在既有清理事务中显式调用知识清理兜底。
- 验证：只读符号核对通过；未写运行数据库、未调用业务 API。
- 结果：PASS
- 下一步：P2.2

### 2026-09-07T03:18:00Z — main-agent Phase 1/P1.2～P1.3
- 修改：新增 `tests/test_product_knowledge.py`；更新 `tests/test_ai_reply.py`、`tests/test_ai_reply_safety.py`，覆盖知识模块可用性、隔离/版本/排序/字符预算、固定策略零模型调用、知识注入和模型输出后置拦截；新增 `app/knowledge/__init__.py` 与 `app/knowledge/passistant_public_v1.json`。
- 旧实现红灯：`python -m unittest discover -s tests -p 'test_product_knowledge.py'` 的模块可用性/知识合同用例失败；AI 回复和安全回归中固定安全句、商业拒答、知识块和主动安全话题拦截用例按预期语义失败；没有用 ImportError 充当行为红灯。原有回归用例仍可独立运行。
- Seed 验证：UTF-8 JSON 解析通过；20 条、8 类、`knowledge_key` 唯一；价格/服区/11 项正式功能/预载/限制/支持覆盖；敏感扫描零命中；原始 SHA-256 `0fefb048817d03e3f9f91f3cc6e6788a60e52143d0b91cb2e5405e31871415ac`。
- 结果：PASS（Phase 1 门禁通过；红灯已记录，待 Phase 2/3 实现转绿）
- 下一步：P2.1

### 2026-09-07T03:05:00Z — main-agent Phase 1/P1.1
- 读取：D 仓库 `AGENT.md`、AI 回复/数据库/API/前端类型与测试文件、`.ai/index/backend-apis.json`；Passistant `AGENTS.md`、架构/文档规则、README、功能清单、官网使用说明、订阅购买指南；当前分支 `main`。
- API 脱敏核验：应用容器已运行且 `healthy`，`GET /health`=200；管理员认证成功，`GET /verify` 已认证；`GET /cookies/details` 返回 1 个账号；`GET /items` 返回 4 件商品，目标 `1081710901648` 唯一匹配 1 件；详情 `GET /items/{cookie_id}/1081710901648`=200，价格 `¥9.90`，`listing_status=on_sale`；`GET /ai-reply-settings/{cookie_id}`=200，`ai_enabled=true`，API key 已配置（只记录布尔值）。
- 静态事实：当前运行源码尚未出现 knowledge 表、检索服务或知识路由接线；Passistant 源仓库只读，未发生文件写入。
- 验证：五个规划 API 均完成状态/数量/非秘密字段核验；未打印密码、Cookie、token、API key、客户数据或完整商品详情。
- 结果：PASS
- 下一步：P1.2

## 生成后对齐与鲁棒性审计

审计时间：`2026-09-07T02:06:46Z`。最终结果：`PASS`。

- 产品源重读：本次用户知识/敏感/安全/价格要求及“客户沟通增加知识库”补充；`<repo-root>/AGENT.md`、`docs/references/ai-rules/01-架构边界.md`、`02-测试规范.md`、`app/ai_reply_engine.py`、`app/db_manager.py`、`app/reply_server.py`、`frontend/components/Sidebar.tsx`、`frontend/App.tsx`、`frontend/components/AIReply.tsx`、`frontend/services/api.ts`、`frontend/types.ts`；外部只读产品文档。
- 文档合同：7/7 文件非空；README 全链接；00 链接 01～03；01～05 前后导航完整；四个 Phase、每个 `P<N>.1` 只读；主代理 fallback 与 `parallelizable_groups: null` 一致。
- 需求覆盖：10 项，覆盖 10，未覆盖 0。明确排除 6 类：自动仓库索引、向量/embedding、商品发布、卡密/发货改造、浏览器/桌面操作、Passistant 上游修改；均有范围依据，不是遗漏。
- 鲁棒性：happy/failure、输入边界、权限安全、幂等重试、并发一致性、部分失败恢复、迁移兼容、资源清理、可观察性和回滚均有 owner、验证与二元完成条件。超时/取消对新 SQLite/API 路径为 N/A（无新外部等待）；既有 DeepSeek 30 秒超时/重试由 TC-AI-12 回归覆盖。
- 审计发现与一次性修复：`CRITICAL` 进度指针缺少注释且字段名错误；`HIGH` 单表无法在空 entries 时保存 version；`HIGH` 检测规避未无条件归 safety；`MEDIUM` 恢复协议、需求 source/section、branch/dependency/done 条件和倒序日志说明不完整；`LOW` README 缺最终审计状态、pricing 示例暗示未来通知。已在 README、00、01～05 对应章节统一修复。
- 未解决 blockers：无。修复后按技能合同不做第二次语义审计；只允许结构回读，因此 PASS 基于本轮完整审计与已应用修复。
- 受保护源：`E:\exilecore2\Passistant` 全部文件、D 项目生产源码/配置/运行数据库均未修改。

### 2026-09-07T02:19:34Z — 用户补充后的计划增量对齐

- 新需求：客户沟通分组新增独立“知识库”入口；当前知识项目命名为“Passistant 知识库”，以后可存在其他项目。
- 证据：当前 `Sidebar.tsx::menuGroups` 的客户沟通包含消息中心、自动回复、AI 回复；`App.tsx` 使用 lazy import、`pageLabels` 和 hidden section 切换页面。
- 调整：把原 AIReply 内嵌编辑器改为独立 `KnowledgeBase.tsx`；新增 sidebar/app 路由接线；documents 增加 `product_key/display_name`，首版每个商品一个知识项目。
- 范围：未引入跨商品共享、多库组合或第三张项目注册表；若未来需要同一商品组合多个知识库，另立迁移计划。
- 结果：增量一致性检查 `PASS`；本次仍只改计划文档，没有修改生产源码或运行状态。

## 新会话执行提示词

```text
请在 `<repo-root>` 执行：
/ecc:execute-doc docs/plan/Passistant轻量商品知识库-2026-09-07/00-执行文档.md

先完整读取该目录七份文档、D 仓库 AGENT.md 和适用 AI 规则，再从进度指针开始，四个 Phase 严格串行。关键合同：
1. 只处理已发布商品 1081710901648；不得创建、发布、复制、编辑或删除第二件商品。
2. Passistant 仓库 E:\exilecore2\Passistant 只读。知识只从当前公开产品文档人工白名单整理，绝不能自动索引源码、内部架构、偏移/内存/签名/检测规避、凭据、成本利润、供应渠道、客户订单或完整激活码。
3. 买家询问封号或安全相关问题时，只回复“目前没有收到玩家反馈有封号情况出现。”；必须在模型调用前确定性返回并证明模型零调用。买家没问时，最终回复不得主动提及封号、安全或检测风险。
4. 商业敏感问题不得直接回答，固定中性回复“这个问题涉及内部信息，暂不提供。”，同样不得调用模型。
5. 普通问题从按 cookie_id + item_id 隔离的 SQLite knowledge documents/entries 两张表中做确定性关键词检索，最多 5 条/2500 字符；空文档也要保留 version；不新增向量库、embedding 或外部依赖。
6. 生产知识导入、回读和 AI 验收只走 http://localhost:8080 认证 API；禁止浏览器、桌面自动化和直接写运行数据库。不要打印密码、Cookie、token、API Key、完整 prompt 或完整激活码。
7. 当前商品合同为原价 ¥29.9/月、当前活动口径 3 折且活动价 ¥9.9/月；¥9.9 不是常规原价，活动价不接受额外砍价，未知活动结束时间不得编造。支持国际服和台服、不支持国服；订单/付款/发货/退款继续由既有订单与自动发货链路处理，知识库不得虚构状态。
8. 先写在旧实现上稳定失败的测试，阶段内修复后跑专用测试；最终跑 Python 全套、前端 npm ci/build、API 索引双生成、git diff --check、docker compose up -d --build、健康检查和 API-only 真实 item 验收。
9. 工作区已有用户改动。不得重置、覆盖或顺手清理；本地提交只暂存本任务文件，不 push。
10. 前端在 Sidebar“客户沟通”组新增独立“知识库”，App 注册 `knowledge-base` 页面；当前项目显示“Passistant 知识库”，稳定 product_key 为 passistant。AI 回复页只保留真实商品回复测试，不内嵌知识编辑器。
```
