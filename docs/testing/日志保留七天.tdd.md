# 日志保留七天：TDD 验证记录

日期：2026-09-13

## 目标

- 应用文件日志只保留最近 7 天。
- 验证码日志按天切分，避免单个 `captcha_verification.txt` 无限增长。
- 自动化测试日志与截图产物只保留最近 7 天。
- 清理操作只处理日志目录的直接文件或测试产物根目录的直接子项，不跟随符号链接。

## RED

先增加 `tests/test_log_retention.py` 和测试产物清理用例，再运行：

```text
python -m unittest discover -s tests -p test_log_retention.py -v
```

结果：失败，`ModuleNotFoundError: No module named 'utils.log_retention'`。

```text
python -m unittest discover -s tests -p test_automation_framework.py -v
```

结果：新增用例失败，`scripts.run_tests` 尚无 `_prune_old_artifacts`。

## GREEN

实现共享的 7 天文件清理器、按日追加日志函数、应用启动清理、Loguru 日切与保留策略，以及测试产物清理后：

```text
python -m unittest discover -s tests -p test_log_retention.py -v
python -m unittest discover -s tests -p test_file_log_collector.py -v
python -m unittest discover -s tests -p test_automation_framework.py -v
```

结果：5 + 12 + 24 项测试全部通过。

完整验证：

```text
python scripts/run_tests.py --suite all --database temp --coverage --quality
```

结果：569 项测试通过；总覆盖率 29.0%；TypeScript、前端构建、Docker Compose 配置和 `git diff --check` 全部通过。

测试产物：`logs/test-artifacts/20260913-020930-all-fe4044fa/`。
